<%= form_for @user, url: "/users", html:{method: :post} do |f| %>
<div class="wrapper2">
    <p class="top3 center">投稿</p>
    <p class="top3">名前：<%= f.text_field :name %></p>
    <p class="top3">年齢：<%= f.text_field :age %></p>
    <input class="top3" type="submit" value="送信">
</div>
<% end %>